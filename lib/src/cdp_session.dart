import 'dart:async';

class CDPSession {

  CDPSession(connection, targetId, sessionId) {
    throw new UnimplementedError();
  }

  Future send(String method, {List params}) async {
    throw new UnimplementedError();
  }
}
